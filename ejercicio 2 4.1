Algoritmo sin_titulo
	definir vector, suma, resta, multi como real
	definir i como entero
	dimension vector(10)
	
	suma=0
	resta=0
	multi=1
	
	escribir 'Ingrese los elementors del vector'
	
	para i=0 hasta 4
		leer vector(i)
	FinPara
	
	para i=0 hasta 4
		suma = suma + vector(i)
		resta = resta - vector(i)
		multi = multi*vector(i)
	FinPara
	
	escribir suma
	escribir resta
	escribir multi
	
FinAlgoritmo
