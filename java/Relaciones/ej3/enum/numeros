package Enum;

public enum Numeros {
    UNO(1), DOS(2), TRES(3), CUATRO(4), CINCO(5), SEIS(6), SIETE(7), DIEZ(10), ONCE(11), DOCE(12);

    private final Integer valor;

    private Numeros(Integer valor) {
        this.valor = valor;
    }
    
    public Integer getValor() {
        return valor;
    }
}
